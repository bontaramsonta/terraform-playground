# GuardDuty ECS runtime monitoring

Enables a GuardDuty detector with the ECS Runtime Monitoring feature
(agent-based container runtime threat detection). Minimal POC — detector +
feature flag; the lambda/caller files are leftovers from the previous
experiment's scaffold.
